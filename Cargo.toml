[package]
name = "tmux-agent-plugin"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Values-only tmux provider for AI agent panes"

[dependencies]
regex = "1.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
